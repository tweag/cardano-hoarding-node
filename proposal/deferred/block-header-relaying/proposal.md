# Block/Header Relaying

_Placeholder — to be written._

Only relevant if the [validation](../../embedded-consensus-validation/proposal.md) proposal is
accepted and the embedded ChainDB approach is adopted. Removing the external
Cardano full node as a dependency means the hoarding node would no longer relay
received blocks and headers to the network, negatively impacting overall network
performance. This proposal would address that gap.
